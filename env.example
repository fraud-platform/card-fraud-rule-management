# App Config
APP_ENV=local
APP_NAME=fraud-governance-api
APP_LOG_LEVEL=INFO

# Region Configuration (P0 - Required for production)
# This is a hard security boundary for regional isolation
# Examples: us-east-1, eu-west-1, ap-southeast-1, india, us, EU, APAC, LATAM
APP_REGION=local

# Optional: point to a local env file (do not commit it)
# ENV_FILE=.env.local

# Testing (recommended)
# - Create a dedicated Neon branch + database (e.g., branch: pytest, db: test_neondb)
# - Create a local env file (e.g., .env.test) with URLs pointing to that DB
# - Run tests with: ENV_FILE=.env.test uv run pytest
#
# Optional: let pytest reset the schema automatically
# - Set PYTEST_RESET_DB=1
# - Ensure the DB name contains "test" or "pytest" (safety guard)
# - Provide DATABASE_URL_ADMIN so resets run with admin privileges
#   (tests will still use DATABASE_URL_APP for normal queries)
# PYTEST_RESET_DB=1

# Database - Runtime app user (used by FastAPI)
DATABASE_URL_APP=postgresql://fraud_gov_app_user:<password>@<neon-host>/neondb?sslmode=require

# Database - Admin user (bootstrap only; you run DDL/seed manually)
DATABASE_URL_ADMIN=postgresql://neondb_owner:<password>@<neon-host>/neondb?sslmode=require

# Auth0 Configuration
AUTH0_DOMAIN=<your-tenant>.us.auth0.com
AUTH0_AUDIENCE=https://fraud-governance-api
AUTH0_ALGORITHMS=RS256

# Auth0 Client Credentials (for test-token endpoint)
# These are used by GET /api/v1/test-token to generate real Auth0 JWT tokens for testing
# Create a Machine-to-Machine application in Auth0 Dashboard with Client Credentials grant
# NOTE: This endpoint is only available in local and test environments (disabled in production)
AUTH0_CLIENT_ID=<your-m2m-client-id>
AUTH0_CLIENT_SECRET=<your-m2m-client-secret>

# CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173